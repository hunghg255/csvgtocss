This file is a merged representation of a subset of the codebase, containing specifically included files, combined into a single document by Repomix.
The content has been processed where content has been compressed (code blocks are separated by ⋮---- delimiter).

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Only files matching these patterns are included: src/**
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Content has been compressed - code blocks are separated by ⋮---- delimiter
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
src/
  templates/
    index.ts
  cli-start.ts
  cli.ts
  errors.ts
  index.ts
  log.ts
  utils.ts
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="src/cli.ts">
import { startCli } from './cli-start'
import { handleError } from './errors'
</file>

<file path="src/errors.ts">
import { consola } from 'consola'
⋮----
export class PrettyError extends Error
⋮----
constructor(message: string)
⋮----
export function handleError(error: unknown)
</file>

<file path="src/log.ts">
export class Log
⋮----
constructor(disabled?: boolean)
⋮----
get disabled()
⋮----
// @ts-ignore
⋮----
set disabled(val: boolean)
</file>

<file path="src/cli-start.ts">
import { svg2Font } from './index';
import { readConfig } from 'unreadconfig';
⋮----
const colorConsoleText = (text: string, color: keyof typeof COLORS) =>
⋮----
export async function startCli(cwd = process.cwd(), argv = process.argv)
</file>

<file path="src/utils.ts">
import path from 'node:path';
⋮----
import fs from 'fs-extra';
⋮----
import { parseXml } from '@rgrove/parse-xml';
import { stringToColor } from '@iconify/utils';
⋮----
/*
 * Filter svg files
 * @return {Array} svg files
 */
export function filterSvgFiles(svgFolderPath: string): string[]
⋮----
/**
 * Tách 1 giá trị màu ra thành { hex, alpha }.
 * Hỗ trợ: hex (#rgb, #rrggbb, #rrggbbaa), rgb()/rgba(), hsl()/hsla(), named color, currentColor.
 */
function parseColorWithAlpha(value: string):
⋮----
// rgba(r,g,b,a) hoặc hsla(h,s,l,a)
⋮----
// #rrggbbaa (hex 8 ký tự, có alpha)
⋮----
// Hex thường, named color... không có alpha riêng
⋮----
function safeNormalize(value: string): string
⋮----
function roundOpacity(value: number): number
⋮----
/**
 * Parse inline style="fill:#000;fill-opacity:0.5;stroke:#fff" thành object key-value.
 */
function parseStyleAttr(style?: string): Record<string, string>
⋮----
/**
 * Lấy danh sách key màu "thực tế render ra" của 1 node cho cả fill và stroke.
 * Opacity = alpha từ giá trị màu (rgba/hex8) * fill-opacity/stroke-opacity riêng (nếu có).
 */
function extractColorKeys(
  attrs: Record<string, string>,
  styleProps: Record<string, string>,
): string[]
⋮----
/**
 * Đệ quy thu thập mọi key màu (fill/stroke + opacity) trong toàn bộ cây SVG,
 * không loại trừ background (rect/circle nền cũng được tính).
 */
function collectColorKeys(node: any, colorKeys: Set<string>): void
⋮----
/**
 * Kiểm tra icon SVG là monochrome (đơn sắc) hay multichrome (đa sắc).
 *
 * Quy tắc:
 * - Không có fill/stroke nào -> monochrome (true)
 * - Chỉ có 1 cặp (màu hex, opacity thực tế) duy nhất trong toàn bộ SVG -> monochrome (true)
 * - Có >= 2 cặp khác nhau (kể cả background khác màu content, hoặc cùng màu nhưng
 *   khác opacity) -> multichrome (false)
 *
 * Không xử lý gradient (linearGradient/radialGradient) - không cần theo yêu cầu hiện tại.
 */
export function svgHasOnlyPathChild(svg: string): boolean
</file>

<file path="src/templates/index.ts">
export const stylesTemplate = (
⋮----
export const genHtml = (
</file>

<file path="src/index.ts">
import path from 'node:path';
import fs from 'fs-extra';
import { cleanupSVG, importDirectory, isEmptyColor, parseColors, runSVGO } from '@iconify/tools';
import { getIconsCSS } from '@iconify/utils';
import { log } from './log';
import { filterSvgFiles, svgHasOnlyPathChild } from './utils';
import color from 'picocolors';
import { genHtml } from './templates';
⋮----
export type SvgToCssOptions = {
  dist: string;
  /**
   * svg path
   * @default svg
   * @example
   * ```
   * ```
   */
  src: string;
  /**
   * The font family name you want.
   * @default icon
   */
  prefix?: string;

  exportJson?: boolean;

  website?: {
    /**
     * website title
     */
    title?: string;
    description?: string;
    template?: string;
    footerInfo?: string;
    links: Array<{
      title: string;
      url: string;
    }>;
  };
};
⋮----
/**
   * svg path
   * @default svg
   * @example
   * ```
   * ```
   */
⋮----
/**
   * The font family name you want.
   * @default icon
   */
⋮----
/**
     * website title
     */
⋮----
interface IDefineConfig {
  src: string;
  dist: string;
  prefix?: string;
  exportJson?: boolean;
}
⋮----
export const defineConfig = (options: IDefineConfig) =>
⋮----
export const svg2Font = async (options: SvgToCssOptions) =>
⋮----
// Import icons
⋮----
// Validate, clean up, fix palette and optimise
⋮----
// Invalid icon
⋮----
// Clean up and optimise icons
⋮----
// Clean up icon code
⋮----
// check svg is monotone
⋮----
// Assume icon is monotone: replace color with currentColor, add if missing
// If icon is not monotone, remove this code
⋮----
// Optimise
⋮----
// Invalid icon
⋮----
// Update icon
</file>

</files>
